import React from "react";
import styles from "./ListHeader.module.css";

const ListHeader = () => {
  return <div className={styles.listHeader__container}>ListHeader</div>;
};

export default ListHeader;
